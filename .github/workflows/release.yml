name: Release

on:
  push:
    tags:
      - 'v*'

# Publish a GitHub Release and upload signed/notarized DMG + ZIP artifacts
# when a version tag (e.g. v0.2.0) is pushed.
#
# Required GitHub repository secrets:
#   CERTIFICATE_P12         — base64-encoded Developer ID Application .p12
#                             Generate: base64 < cert.p12 | tr -d '\n'
#   CERTIFICATE_PASSWORD    — password for the .p12
#   APPLE_ID                — Apple ID email used for notarization
#   APPLE_APP_SPECIFIC_PASSWORD — app-specific password from appleid.apple.com
#   APPLE_TEAM_ID           — 10-character Team ID from developer.apple.com
#   (GITHUB_TOKEN is provided automatically)

jobs:
  build:
    runs-on: macos-14   # Apple Silicon — can cross-compile x64 too
    name: Build (arm64 + x64)

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: electron/package-lock.json

      # ── Install qmd (Node.js script — same file works for both arches) ──────
      - name: Install qmd
        run: npm install -g @tobilu/qmd

      - name: Bundle qmd binary
        run: |
          mkdir -p electron/resources/bin
          QMD="$(which qmd)"
          cp "$QMD" electron/resources/bin/qmd-darwin-arm64
          cp "$QMD" electron/resources/bin/qmd-darwin-x64
          chmod +x electron/resources/bin/qmd-darwin-arm64
          chmod +x electron/resources/bin/qmd-darwin-x64

      # ── Install Electron dependencies ─────────────────────────────────────
      - name: Install dependencies
        run: npm ci
        working-directory: electron

      # ── Import Developer ID certificate into an ephemeral keychain ────────
      - name: Import signing certificate
        env:
          CERTIFICATE_P12: ${{ secrets.CERTIFICATE_P12 }}
          CERTIFICATE_PASSWORD: ${{ secrets.CERTIFICATE_PASSWORD }}
        run: |
          KEYCHAIN_PATH="$RUNNER_TEMP/build.keychain"
          CERT_PATH="$RUNNER_TEMP/certificate.p12"
          echo "$CERTIFICATE_P12" | base64 --decode > "$CERT_PATH"
          security create-keychain -p "" "$KEYCHAIN_PATH"
          security set-keychain-settings -t 3600 -u "$KEYCHAIN_PATH"
          security unlock-keychain -p "" "$KEYCHAIN_PATH"
          security import "$CERT_PATH" -k "$KEYCHAIN_PATH" -P "$CERTIFICATE_PASSWORD" \
            -T /usr/bin/codesign -T /usr/bin/security
          security set-key-partition-list -S apple-tool:,apple: -s -k "" "$KEYCHAIN_PATH"
          security list-keychains -d user -s "$KEYCHAIN_PATH" $(security list-keychains -d user | tr -d '"')
          rm "$CERT_PATH"

      # ── Build arm64, then x64 — both signed, notarized, uploaded ─────────
      - name: Build and publish (arm64)
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run dist:mac:arm64
        working-directory: electron

      - name: Build and publish (x64)
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run dist:mac:x64
        working-directory: electron
